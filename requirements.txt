# Dependencies for the VIX LSTM forecasting notebook.
# Install with:   pip install -r requirements.txt
#
# Versions below are what the notebook was verified against.
# Pinned with >= because Yahoo Finance and TF API can shift slightly
# between minor versions, but the script is tolerant.

numpy>=1.24
pandas>=2.0
tensorflow>=2.12         # use tensorflow-cpu if you don't have a GPU
scikit-learn>=1.3
yfinance>=0.2.40
matplotlib>=3.7
